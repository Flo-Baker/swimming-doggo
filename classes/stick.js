/* 1. creating the Stick class which needs the position (this.x/y/h/w), the speed on the 
X axis, the img size and the img src

class Stick {
    constructor(x, y, height, weight, speed, image, image.src) {
        this.x = ;
        this.y = ;
        this.height = ;
        this.weight = ;
        this.speedX = ;
        this.image = ;
        this.image.src = ;
    }
}
*/

// 2. FUNCTIONS THAT ARE NEEDED

// drawSeal () {}