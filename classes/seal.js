/* 1. creating the Seal class which needs the position (this.x/y/h/w), the speed on the 
X axis (because of the Movement there), the img size and the img src

class Seal {
    constructor(x, y, height, weight, speed, image, image.src) {
        this.x = ;
        this.y = ;
        this.height = :
        this.weight = ;
        this.speedX = ;
        this.image = ;
        this.image.src = ;
    }
}
*/

// FUNCTIONS THAT ARE NEEDED FOR THE SEAL

// 2. drawSeal () {}